==================================
 dotfiles, ~/bin, and static sites
==================================

.vimrc
    vim configuration
.gvimrc
    macvim configuration
.profile
    bash (shell) configuration

bin/
    shell scripts

<3
    you

